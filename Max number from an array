#include <stdio.h>
#include <stdlib.h>

int main()
   { int a[10] , max ,i;
   for(i=0;i<9;i++)
    {scanf("%d ",&a[i]);
    }

   max=a[i];
   for(i=1;i<=10;i++)
   {
    if(a[i]<a[i+1])
        max=a[i+1];
   }

    printf("\n max is %d",max);
    return 0;
}
